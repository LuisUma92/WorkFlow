# Plan — reviewer-esquema follow-ups (v1.14.0–v1.17.0)

> Source backlog: `tasks/requests/2026-05-29-v1.14.0-reviewer-esquema-followups.md` (10 items).
> Method: each wave shipped TDD → parallel python+architect review → fix CRITICAL/HIGH
> inline → commit GREEN. Isolated `WORKFLOW_DATA_DIR`. Model routing opus=review,
> sonnet=code, haiku=git.

## Severity / domain map

| # | Item | Sev | Domain | Wave |
|---|------|-----|--------|------|
| 4 | `BibEntry.year` fixture int-consistency | LOW | exercise tests | 1 |
| 6 | extract `graph.node_ids` helper | LOW | graph | 1 |
| 1 | shared neutral base for `BibKeyAmbiguous` | MED | errors | 2 |
| 5 | promote `EntityNotFoundError`/`UniquenessError` → `workflow.db.errors` | MED | errors | 2 |
| 2 | ADR for `workflow.bibliography` boundary | LOW | ADR | 2 |
| 3 | prisma re-export removal milestone | LOW | ADR/bib | 2 |
| 9 | shared duplicate-lookup helpers (`get_topic_by_serial`/`get_content_by_name`) | MED | importer | 3 |
| 8 | move bulk-import → neutral `workflow/importer/`, `workflow import` verb | HIGH | importer | 3 |
| 10 | document concept global-skip + revisit SAVEPOINTs | LOW | importer docs | 3 |
| 7 | resolve `relation_type=edge_type` overload | MED | graph contract | 4 |

## Sequencing rationale
- **Front-load zero-coordination quick wins** (Wave 1) — no ADR, no decisions, builds momentum.
- **Batch the ADR-0007 amendment** — items 2 and 5 both amend ADR-0007; do it ONCE in Wave 2.
- **Error taxonomy is the linchpin** — items 1+5 are mutually dependent ("Phase 1.5"); land them
  together, then the bib-boundary governance (2+3) rides the same ADR sweep.
- **Importer refactor needs 9 before 8** — shared lookups must exist so the moved importer reuses
  them; item 10's `--help` doc lands AFTER 8 renames the command (avoid rework).
- **Graph contract (7) last** — breaking JSON-key change needs a locked decision + 3-surface
  coordination (cli + test + Lua picker + ADR-0017).

---

## Wave 1 — Quick wins (items 4, 6) · LOW · ~1 session
**4.** Align exercise test fixtures `year="2020"`→`year=2020` (int) in
`tests/workflow/exercise/test_cli.py`, `test_service.py`. No prod change.
**6.** New `src/workflow/graph/node_ids.py`: `is_note(node_id) -> bool`,
`parse_note_id(node_id) -> int | None`. Refactor the ×4 `startswith("note:")` guards +
`_fetch_note_rows` split + `_note_path` in `graph/cli.py` to use it. Unit tests for the helper.
- Risk: minimal. No decisions. No ADR.

## Wave 2 — Error taxonomy + bib-boundary ADR sweep (items 1, 5, 2, 3) · MED · ~1–2 sessions
**1+5.** Create `src/workflow/db/errors.py` with neutral bases:
`EntityNotFoundError`, `UniquenessError`, and a neutral `AmbiguousLookupError`.
- Re-home `ContentServiceError`'s `EntityNotFoundError`/`UniquenessError` (currently in
  `content/service.py`) to inherit from the neutral bases so `except ContentServiceError`
  STILL catches everything (verify with a test).
- Make bibliography-layer `BibKeyAmbiguous` and content-layer `BibKeyAmbiguous` share the neutral
  `AmbiguousLookupError` base; convert the boundary in `content/bib_links.py::_resolve_bib_entry`
  from re-message → `raise ContentBibKeyAmbiguous(...) from exc`.
- **[D2 LOCKED]** `BibLinkNotFound` stays **standalone** under `ContentServiceError` (architect:
  a missing *link* ≠ a missing *entity*) — do NOT fold into `EntityNotFoundError`.
**2.** Write `docs/ADR/0020-bibliography-module-boundary.md` (1 paragraph): foundation/lookup layer,
prisma re-export = deprecation path, 0/1/2+ lookup contract.
**3.** `prisma/service.py:13` re-export — audit remaining external callers, add a removal milestone
note. Keep `_bib_entry_options` (genuine internal dep — do NOT remove; see lessons.md).
**ADR-0007 amendment (once):** "service-layer error taxonomies introduced in a feature module are
provisional; generic bases graduate to `workflow.db.errors` once a 2nd consumer appears" +
"read-heavy CLI surfaces MAY bypass repo protocols."
- Risk: cross-layer catch compatibility — guard with explicit `except ContentServiceError` tests.

## Wave 3 — Importer extraction (items 9 → 8 → 10) · HIGH · ~2 sessions
**9 (first).** Extract `get_topic_by_serial(da_id, serial)` and `get_content_by_name(topic_id, name)`
into the topic/content services; use them in BOTH the add-guards AND `bulk_import.py` (kill the
drifting duplicate predicates). Additive, low risk.
**8.** Move `workflow/topic/bulk_import.py` → new neutral `workflow/importer/` package
(cross-domain composition root: topic+content+concept). Expose `workflow import` (verb imports a
hierarchy rooted at DisciplineArea). Keep `workflow topic import` as a **deprecation alias**.
- **[D3 LOCKED]** package = `workflow/importer/`, verb = `workflow import`.
**10 (after 8).** Document concept global-skip (reused `code` under a different content is silently
skipped, not re-linked) in `workflow import --help` + the YAML template. Revisit per-row SAVEPOINTs
(ADR-0018) if the global engine adopts the pysqlite BEGIN recipe.
- Risk: 8 is a module move with a public-CLI surface — highest blast radius; full-suite gate +
  alias test. Unblocks a 2nd import surface (bib/notes).

## Wave 4 — Graph contract overload (item 7) · MED · ~1 session
`graph neighbors --json` emits `relation_type=<GraphEdge.edge_type>` (knowledge type
`link`/`citation`), colliding with NoteEdge semantic `relation_type` (`supports`/`contradicts`).
- **[D1 LOCKED]** RENAME key `relation_type`→`edge_type` (breaking JSON contract).
- Update ADR-0017 + all three surfaces together: `graph/cli.py`, its test, the Lua picker.
- Risk: breaking JSON contract — coordinate the 3 surfaces in one commit.

---

## Decisions — LOCKED 2026-06-02
- **D1 (Wave 4):** RENAME key `relation_type`→`edge_type` in `graph neighbors --json`
  (breaking; update ADR-0017 + cli + test + Lua picker together).
- **D2 (Wave 2):** `BibLinkNotFound` stays **standalone** under `ContentServiceError`
  (architect: a missing *link* ≠ a missing *entity*) — do NOT fold into `EntityNotFoundError`.
- **D3 (Wave 3):** package = `workflow/importer/`, verb = `workflow import`
  (hierarchy rooted at DisciplineArea); `workflow topic import` kept as deprecation alias.

## Acceptance
- Each item closed or folded into its wave; backlog file flipped to `status: completed`.
- Suite GREEN + flake8 0 at every wave commit. ADR-0007 amended once; ADR-0017/0020 updated.

---
status: proposed
date: 2026-05-29
title: Post-v1.13.1 Roadmap — reviewer-esquema deferred items + open gaps
---

# Roadmap — 2026-05-29 (post-v1.13.1)

## Scope

Addresses the 3 architectural-debt requests filed 2026-05-29 from the
4-reviewer-esquema synthesis of v1.11.0–v1.13.0 work, plus 2 pre-existing
open gap requests still unscheduled:

- `tasks/requests/2026-05-29-bibliography-service-extraction.md` — HIGH
- `tasks/requests/2026-05-29-content-service-split.md` — HIGH
- `tasks/requests/2026-05-29-nvim-plugin-plenary-harness.md` — MED-HIGH
- `tasks/requests/2026-05-28-graph-neighbors-json.md` — MAJOR (open since 2026-05-28)
- `tasks/requests/2026-05-28-nvim-plugin-taxonomy-coverage.md` — partial; Tier-2 `:WorkflowGraphNeighbors` blocked

Excludes:

- `tasks/requests/2026-04-22-wiki-single-vault-refactor.md` — kept from
  prior roadmap; unchanged status
- Personal plans outside the repo (`~/.claude/plans/*`)
- `tasks/lessons.md` and `tasks/todo.md` (perpetual logs)

## Phase 0 — Triage pass (15 min, do FIRST)

Two items may already be obsolete after v1.13.1; verify:

- `2026-05-29-bibliography-service-extraction.md` — v1.13.1 already
  removed the actual cross-import (single-select refactor bypassed the
  prisma helper). Is the *extraction* still motivated for future use,
  or can this be closed as "no current consumer"? Decision: **keep
  proposed** — `get_bib_entry_by_bibkey` still lives in `prisma.service`;
  future bib commands will re-need it.
- Plugin-side Tier-3 `:WorkflowLectureBuildEval` from
  `2026-05-28-nvim-plugin-taxonomy-coverage.md` — CLI signature is
  multi-arg taxonomy; revisit whether to ship as positional or kv-args,
  or to close as "shell-only by design."

Outcome: 0 items move to `completed`; 1 may move to `wontfix`.

## Phase 1 — Architectural refactor bundle (1–2 days) → tag `v1.14.0`

CLI-side architectural cleanup. Land both together; order matters
because content-split's new `bib_links.py` should import from the new
bibliography module, not prisma.

| Step | Item | Effort |
|------|------|--------|
| 1a | `2026-05-29-bibliography-service-extraction.md` — create `src/workflow/bibliography/service.py`; move `get_bib_entry_by_bibkey` + `_bib_entry_options`; deprecation re-export in `prisma.service` | S |
| 1b | `2026-05-29-content-service-split.md` — split `content/service.py` (177 LOC) into `service.py` (CRUD) + `bib_links.py` (link mgmt); re-exports in `content/__init__.py` | S-M |
| 1c | Update `content/bib_links.py::_resolve_bib_entry` to call the new `bibliography.service` helper (cleaner than the single-select pattern shipped in v1.13.1, now that the helper has a proper home) | XS |

Verification: 1410+ tests stay green; `grep -rn "from workflow.prisma" src/workflow/content/` returns zero; reviewer-esquema (python + architect) at the end of the bundle.

Decision needed before starting: should `_resolve_bib_entry` in step
1c revert to the helper-based pattern (cleaner), or keep the v1.13.1
single-select (eliminates the count + select two-roundtrip)? Both
work; the helper version reads better but the single-select is
faster. Mention in step 1c that this is a stylistic choice.

## Phase 1.5 — Error class consolidation (½ day) → `v1.14.1`

Architect MEDIUM finding (deferred from reviewer-esquema):

Introduce intermediate base classes under `ContentServiceError`:
- `EntityNotFoundError` ← `TopicNotFound`, `ContentNotFound`, `BibEntryNotFound`
- `UniquenessError` ← `DuplicateContent`, `BibLinkAlreadyExists`
- (keep `BibKeyAmbiguous` and `BibLinkNotFound` standalone)

Shrinks the import list in `cli.py`. No file count: `content/service.py`
+ `content/bib_links.py` updated; CLI handlers collapsed.

Bundle only if Phase 1 doesn't ship error renames; otherwise fold in.

## Phase 2 — Plugin test harness P1+P2 (2–3 days) → tag `v1.15.0`

Land `2026-05-29-nvim-plugin-plenary-harness.md` Phases P1+P2 only:

| Sub-phase | Deliverable |
|-----------|-------------|
| P1 | `nvim-plugin/tests/plenary/minimal_init.lua`; one picker spec proof (`topics_spec.lua`) covering contract + insert + empty paths; `make test-plugin` target in repo Makefile |
| P2 | Spec files for the other 6 pickers (`contents`, `concepts`, `content_bib`, `edges`, `notes`, `evaluations`) — ~3 cases each |

Verification: `make test-plugin` exits 0; ≥ 21 Lua specs pass; existing
`scripts/smoke_taxonomy.sh` continues to pass.

## Phase 3 — Plugin test harness P3+P4 (2 days) → tag `v1.15.1`

| Sub-phase | Deliverable |
|-----------|-------------|
| P3 | `frontmatter_spec.lua` (YAML edge cases — hand-rolled parser long-untested); `commands_spec.lua` (kv-arg regex, hyphenated keys, value sanitization); `server_spec.lua` (basename allowlist from v1.13.1) |
| P4 | CI integration (GitHub Actions step running plenary on at least one nvim 0.10.x build); optional coverage reporting |

Verification: CI green on master + at least one branch with a
deliberate Lua regression that the harness catches.

## Phase 4 — Open gap closure (1 day) → tag `v1.16.0`

Close pre-existing gap requests from prior session:

| Step | Item | Effort |
|------|------|--------|
| 4a | `2026-05-28-graph-neighbors-json.md` — add `--json` flag to `workflow graph neighbors`; return `{"source": {...}, "neighbors": [...]}` with `path`, `depth`, `edge_class`, `relation_type` keys per request | S |
| 4b | Plugin: implement `:WorkflowGraphNeighbors {note-id}` per the deferred Tier-2 plan in `2026-05-28-nvim-plugin-taxonomy-coverage.md`; Snacks picker, `<CR>` opens neighbor's `path` | S |
| 4c | Close both requests; update `smoke_taxonomy.sh` with a `check_json` entry for `graph neighbors --json` | XS |

Test coverage: at least 4 new tests on the Python side
(`tests/workflow/graph/test_neighbors_json.py`) + ≥ 1 plenary spec on
the plugin side (depends on Phase 2 harness landing first).

## Cross-cutting deferred (no phase — file follow-ups later)

- ADR-0007 amendment: document that service-layer modules may bypass
  `workflow.db.repos.protocols` for read-heavy CLI surfaces (architect
  observation; current code already follows this de-facto rule).
- Naming asymmetry: `concept` vs `topic`/`content` top-level CLI groups
  — future ADR if a fourth peer (e.g., `discipline`) lands.
- `2026-04-22-wiki-single-vault-refactor.md` residual: stale
  `00ZZ-Vault` reference at `docs/wiki/Zettelkasten-Notes.md:109` — 10-line
  micro-fix; carry into any docs sweep.

## Effort summary

| Phase | Tag | Effort | Risk |
|-------|-----|--------|------|
| 0 | — | 15 min | none |
| 1 | v1.14.0 | 1–2 days | LOW (refactor only; full test suite catches regressions) |
| 1.5 | v1.14.1 | ½ day | LOW |
| 2 | v1.15.0 | 2–3 days | MEDIUM (new infra; plenary setup) |
| 3 | v1.15.1 | 2 days | LOW once Phase 2 lands |
| 4 | v1.16.0 | 1 day | LOW |

**Total: ~7–9 working days.**

## Sequencing rationale

- **Phase 1 + 1.5 first** — pure refactor; no user-visible changes;
  shrinks `content/service.py` before any new link types land
- **Phase 2 before Phase 4** — Phase 4 wants to add a plenary spec
  for the new `:WorkflowGraphNeighbors`; needs the harness to exist
- **Phase 3 last in the harness chain** — frontmatter + commands +
  server specs are higher value once we know the picker pattern is
  testable
- **Phase 4 anytime after Phase 2** — independent of refactor, but
  cheaper to schedule near the test harness

## Acceptance criteria for closing the roadmap

- All 5 requests closed or moved to wontfix
- v1.16.0 tagged on `public`
- Reviewer-esquema run at end of each phase that touches ≥ 3 files
  (Phase 1, Phase 2, Phase 4)
- `tasks/lessons.md` updated with at least 1 row per phase
- This file flipped to `status: completed`
